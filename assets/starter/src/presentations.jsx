import React from 'react';
// Technical fixture, not a claim about the target product.
// Replace with the feature-level original components, their styles and controlled states.
// Copying tokens or removing the placeholder marker is not a valid integration.
export function FeatureVisual() {
  return <div className="neutral-fixture" data-enter="0.10" data-skill-placeholder="true">
    <span className="eyebrow">COMPONENT ADAPTER</span>
    <h2>把真实组件放到这里。</h2>
    <p>接入产品组件后，用固定的展示状态驱动动画。</p>
    <div data-enter="0.45" className="fixture-row">真实组件 → 受控状态 → 镜头动作</div>
  </div>;
}
